type IsFocusType = {
	isFocus: boolean;
};

type IsErrorType = {
	isError: boolean;
};

type IsCurrentType = {
	isCurrent: boolean;
};

type IsHiddenType = {
	isHidden: boolean;
};

type IsFixedType = {
	isFixed: boolean;
};

type CustomCSSType = {
	customCSS: string;
};

type IsFailedType = {
	isFailed: boolean;
};

type IsSelectedType = {
	isSelected: boolean;
};

type IsDisabledType = {
	isDisabled: boolean;
};

type IsGrayType = {
	isGray: boolean;
};

type IsScrollExistType = {
	isScrollExist: boolean;
};

type IsOpenType = {
	isOpen: boolean;
};

type IsWhiteType = {
	isWhite?: boolean;
};

type IsExceptionType = {
	isException?: boolean;
};

type WidthType = {
	width?: string;
};

type IsShownType = {
	isShown?: boolean;
};

type BottomType = {
	bottom?: string;
};

type IsHoverType = {
	isHover: boolean;
};

type IsCheckedType = {
	isChecked: boolean;
};

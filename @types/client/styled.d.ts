type IsFocusType = {
	isFocus: boolean;
};

type IsErrorType = {
	isError: boolean;
};

type IsCurrentType = {
	isCurrent: boolean;
};

type IsHiddenType = {
	isHidden: boolean;
};

type IsFixedType = {
	isFixed: boolean;
};

type CustomCSSType = {
	customCSS: string;
};

type IsFailedType = {
	isFailed: boolean;
};

type IsSelectedType = {
	isSelected: boolean;
};

type IsDisabledType = {
	isDisabled: boolean;
};

type IsGrayType = {
	isGray: boolean;
};

type IsShowScrollType = {
	isShowScroll: boolean;
};

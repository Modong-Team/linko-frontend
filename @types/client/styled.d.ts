type IsFocusType = {
	isFocus: boolean;
};

type IsErrorType = {
	isError: boolean;
};

type IsCurrentType = {
	isCurrent: boolean;
};

type IsHiddenType = {
	isHidden: boolean;
};

type IsFixedType = {
	isFixed: boolean;
};

type CustomCSSType = {
	customCSS: string;
};
